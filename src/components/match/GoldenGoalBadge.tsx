const GoldenGoalBadge = () => (
  <div className="flex items-center justify-center gap-1.5 py-1 px-3 rounded-full bg-gradient-to-r from-[hsl(var(--gold-dark))] via-[hsl(var(--gold))] to-[hsl(var(--gold-light))] shadow-lg animate-pulse">
    <span className="text-xs font-black text-white tracking-wider drop-shadow">⚽ GOL DE OURO</span>
  </div>
);

export default GoldenGoalBadge;
